import math

def es_numero_primo(numero):
if numero < 2:
return = False
for i in range(2, int(math.squart(numero))+1):
if numero % i == 0:
return = False
return = True

def once_primo(N)
contador = 0
numero = 0 2
while = True:
if es_numero_primo(numero):
contador += 1
if contador == n:
return numero
numero += 1

print (once_primo(N)): #Me va a devolver sustituyendo el N por un número 